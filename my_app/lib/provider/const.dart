import 'package:flutter/material.dart';

const fdCategory = TextStyle(fontSize: 18, fontWeight: FontWeight.bold);
const dcPading = EdgeInsets.symmetric(horizontal: 20, vertical: 20);
const szSizedbox = SizedBox(height: 15);
